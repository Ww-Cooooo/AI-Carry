# 升级到 AI Carry 2.1.3

2.1.3 主要整理记忆系统的公开边界：公开版本保留本地可读文件、可重建派生索引和回退原则；完整的记忆引擎评估线、RAG／向量数据库／知识库接入与实施手册属于会员或定制服务内容。

已有实例从“检查并升级 AI Carry”进入。实例身份、记忆正文、能力、SOP、经验、学习候选、Skill、工作区、本机绑定和私密资料保持原位；本次不把用户的记忆搬到数据库，也不因公开说明变化重写已有资产。

升级前后都应保留 Markdown/TOML 真源。若用户已经有本地派生索引，继续按原实例的维护方式使用；索引失效只影响检索层，可以回到文件地图，不应阻断对话或普通任务。

完整的 VIP 记忆系统升级手册不随公版分发。用户需要全文检索、混合检索、RAG、向量数据库、飞书或其他知识库接入时，由 Agent 先说明范围、资源、隐私和回退方式，再按单独的会员或定制服务约定实施。

这是一次公开文档与内容边界更新，不要求迁移用户资料。完成后核对版本、启动胶囊和一项与当前实例相关的无破坏行为；不要把缺少 VIP 资料当成公版安装或升级失败。
