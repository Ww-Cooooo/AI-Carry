# 模板升级指南

仅在用户明确要求检查或应用模板更新时读取。实例运行良好时不强制升级。进入本路线时同时读取 `core/protocols/INSTANCE_EVOLUTION_COMPATIBILITY.md`，让升级计划真实遵守同一组所有权、保留、适配、重连和局部隔离原则；不能只在文档里提到协定而不用于当前实例方案。

## 用户看到的简单流程

1. 用户直接说“帮我检查 AI Carry 有没有官方新版本”（旧实例说 Agent Carry 也能识别），或在看板中复制升级请求；不需要自己寻找版本清单。
2. Agent 读取当前实例的 `core/upgrade/official-source.toml` 只为取得官方来源地址和核验规则。当前旧实例自带的 `latest_release` 与 `core/upgrade/` 清单只是它安装时已知的历史，不能通过枚举其中最高版本断言线上没有新版。允许联网时，只从登记的官方公开仓库和正式 Release 确定最新标签，再取得该标签对应的 GitHub 自动生成源码 ZIP；只有已经证明公开 `main` 与该标签指向同一提交时，才可改用登记的“最新版 ZIP”短链接。如果当前请求已经提供唯一的本地升级包或解压树，先按同一来源、发布清单、路径与逐文件规则核验这份目标并直接使用；无法联网且没有唯一目标时，才请用户提供本地升级包。
3. Agent 告诉用户当前版本、已核验目标版本和来源。目标包自报的身份与发布边界只证明包内声明，不能单独冒充官方来源。目标版本如果登记了随包确定性 `prepare` 入口，该入口从登记的 GitHub HTTPS API 现场核对非草稿、非预发布 Release、固定标签提交、目标 Git 树、目标逐文件字节和发布清单；Agent 只把返回的 `userPreview` 原样展示。`prepare` 不替换实例，也不等于用户同意升级。用户确认后，`confirm` 只核对同次预览绑定的本地来源、目标树与实例没有漂移，不重复联网。
4. 用户选择：保留现状，或迁移升级。
5. Agent 自动执行、验证并报告；用户不需要操作终端。

正式 Release 的固定标签与内容才是这次安装来源。公开 main 前进不表示该 Release 失效；main 不再指向该标签时改用标签 ZIP，不能把开发中的 main 快照当成正式版。请求次数只限制联网开销，不是安装内容的正确性条件。

检查更新只在用户明确提出时进行。AI Carry 不后台联网、不自动下载，也不会因为发现新版本就强制升级。公开仓库不需要 GitHub 登录；如果用户指定其他仓库、附件或镜像，Agent 必须明确说明它不是登记的官方来源，并在读取或下载前让用户决定。

当前看板的正式支持目标是电脑端离线使用。升级后的界面验收以常见电脑窗口为准；手机窄屏布局不属于当前承诺，也不能因为手机端裁切就判定实例升级失败或停止其他能力。Agent 应直接向用户说明这一边界，不假装已经适配手机。

## 预览

Agent 读取当前 `instance/manifest.toml`、`core/upgrade/official-source.toml`、目标发布清单和 `core/ownership.toml`，将文件分为：

- 模板核心：可按清单替换或迁移。
- 实例身份与个人资产：默认保留。
- 正在使用的扩展接口：检查兼容范围。
- 本地私密和派生文件：不从升级包覆盖。

专业工作区、组件、Skill 和本机工具默认留在原位；只有本次变更实际影响其接口或准备使用它时，才读取相关清单并检查兼容。缺少登记或可选组件异常只影响该项，不因存在一个未知目录就阻止核心升级，更不能猜测、执行或删除其中内容。

源实例不是干净发布包：规划目录、本地 `AGENTS.override.md`、工具锁文件和已安装依赖可以属于实例，保留即可，不要求为通过检查把它们搬走。明确的维护者目录、身份冲突或本次触碰范围不可信时，才暂停对应切换；目标发布包仍必须是没有私密开发资料和本地依赖的干净模板。

如果现有实例已经有 `.assistant-private/catalog.toml`、`bindings.toml` 或私密正文，预览必须把它们列为“实例本地私密状态，原样保留”；模板只替换公开 Schema 与空模板，不创建、重置或猜测当前设备绑定。旧实例没有目录文件不构成升级失败：只有以后登记资料、命中 `private_refs`、导入导出或完整迁移时，才从当前相关引用渐进建立。

如果用户改过模板核心，先在隔离候选按实例兼容协定判断能否转换为已有扩展点、组件适配器或版本化迁移；原件保持不动。确实无法安全表达时，明确提示“该实例存在尚不能兼容迁移的核心修改”，保留旧实例并给出具体冲突，不让用户面对逐行合并工具。

预览还要检查一个旧版兼容问题：有些早期实例把完整个人档案直接写在 `instance/profile/README.md`，并让 `profile.user_preferences_ref` 指向它；新模板又把同一路径当作目录说明。发现这种结构时，预览必须明确显示“先把个人档案原样迁到 `instance/profile/approved-profile.md` 并更新引用，再替换目录说明”，不能把 README 直接覆盖。目标文件已有不同内容时显示冲突并停止自动迁移。

## 两个选择

- **保留现状**：跳过本次升级，实例继续工作。
- **迁移升级**：保留当前实例根，只替换目标清单拥有的产品文件和精确列出的目录说明；用户身份、记忆、能力、SOP、Skill、工作区与私密内容不搬家、不重建、不推断删除。版本与启动胶囊按执行器迁移；其他实例内容仅在当前版本有明确迁移需要时有界处理，例如旧档案引用和内置治理卡的已知品牌短语，排期与用户正文仍保留。升级不是重新实例化，也不是把全部旧资产按新 Schema 重写。

Agent 从已核验的目标发布包组装隔离候选，只为实际变化的核心文件保留回滚前像，不复制整棵用户目录。切换前检查实际触碰路径及必要平台元数据；核心写入或回读失败时恢复当前耦合写集。Windows 不默认复制系统审核权限或所有者，不因失败擅自提权。目标发布包不能混入开发依赖、Git 元数据、缓存或维护者私密文件。快捷方式或看板入口缺失时给出正确路径或定向修复，不把它作为核心升级的先决条件。

隔离目录只证明候选文件与正式实例分开，不证明候选调用的本机运行时也分开。代表行为需要启动组件或专业扩展绑定的本机工具时，Agent 先用 `dashboard/scripts/copied-instance-runtime-boundary.mjs` 核对正式源根、候选根和本次明确使用的少量绑定文件。任何绑定回指正式源实例、未核验共享运行时或未解析动态路径时，不运行该工具，只把这项能力记为 `limited`，继续完成其他静态和无关行为检查；可以在候选中重绑副本专用运行时后重试。预检不得改写正式实例，用户也不需要手工运行它。

核心切换后，Agent 根据当前实例真实内容刷新两份一致的看板快照，不能用目标模板空白数据覆盖实例。快照失败只让看板进入待刷新并给出定向重试，核心升级和普通对话继续。刷新成功后再从稳定入口核对快捷方式、离线资源、匿名身份胶囊和页面标题；能读取地址栏时再核对精确落点。真正的核心身份或入口目标不一致才停止并恢复触碰过的核心文件。

升级后只验证新核心、实例身份与本次实际受影响的能力，再完成一项相关的无破坏代表行为。未变更的资产、组件、提醒和宿主档案不做全库回归，不因旧字段缺失要求重写；相关内容真正被使用或修改时再定向适配。正常召回仍走地图到命中正文，不整批加载上下文。成功候选可按执行器规则清理，回滚包保留到用户明确结束保护，失败现场不自动删除。

核心切换后按 `core/protocols/UPGRADE_SESSION_ACTIVATION.md` 分开报告文件升级、看板刷新和当前会话采用。执行同次 `sessionReentryCommand` 核对回滚包、胶囊、manifest 和代表路线；双快照状态单独报告，待刷新时另用返回的定向命令修复，不阻止接续新版规则。CLI 不能自行生成 `sessionActivated=true` 或 `behaviorAccepted=true`；宿主能观察真实重载与代表行为时再闭合，不能时说明下一次自然打开会采用新版。看板、会话或单项行为待接续都不回滚有效实例；只有核心文件或启动真源无效才回滚。

实例升级验收只使用这次升级返回的 `sessionReentryCommand`，再继续当前工作或完成一项与用户目标相关的无破坏代表行为。`dashboard/package.json` 中的 `check:*` 是母版开发、构建和发布工具，不属于已安装实例的升级门禁；不得在实例中枚举或运行整套开发检查，也不得因为缺少 `node_modules`、构建依赖或维护者环境把已经可用的实例判成升级失败。

从 1.2.1 升到 1.3.1 时还要完成四项定向迁移：为 frontmatter 与直接路线成对补齐保守确认门；把没有真实旧批准证据的 policy 授权资产留在复核；用现有真实结果建立 `instance/validations/index.toml`，旧成熟度无法闭合时进入“需要证据／复核”而不是补造记录；严格回读 manifest 后，在同一可恢复事务中重建 `instance/startup-capsule.toml` 并核对源摘要。风险分级只影响候选验证和复核顺序，不会在升级过程中自动采用正式资产。详细规则见 `core/upgrade/upgrade-1.2.1-to-1.3.1.md` 与 `core/upgrade/release-manifest-1.3.1.toml`。

从 1.3.0 升到 1.3.1 不重新执行上述旧资产迁移，只更新模板拥有的产品文件和实例产品版本，再重建启动胶囊与两份真实快照。1.3.1 修复发布 authority，并在继续禁止整个 `.assistant-local`／`.assistant-private` 的前提下，只允许发布清单逐字列出的九个普通零字节 `.gitkeep`。详细规则见 `core/upgrade/upgrade-1.3.0-to-1.3.1.md`；任何新的公开提交、标签、Release 或 Pages 仍需独立授权。

1.3.1 → 1.4.0 增加实例持续进化兼容协定、小型组件登记和旧实例一次性纳管；详细规则见 `core/upgrade/upgrade-1.3.1-to-1.4.0.md` 与 `core/upgrade/release-manifest-1.4.0.toml`。该历史发布保持不变。

1.4.0 → 1.4.1 修复首次实例化身份事务闭包：尚未实例化的 1.4.0 模板升级后仍保持空模板，并必须能够从同一真源完成通用和领域两条首次创建链；已经实例化的 1.4.0 只更新产品版本并重建严格启动胶囊与真实双快照，身份、方向、档案、资产、验证记录、候选、组件、专业扩展、工作区、本机绑定和私密状态继续保留。详细规则见 `core/upgrade/upgrade-1.4.0-to-1.4.1.md` 与 `core/upgrade/release-manifest-1.4.1.toml`。该历史发布保持不变。

1.4.1 → 1.4.2 只稳定实际使用回执、阶段学习回执和回复末尾的用户行动引导，不改变现有 Schema、组件接口、实例持续进化协定或首次实例化事务。空模板保持空白；已经实例化的 1.4.1 保留全部实例身份、档案、资产、验证、候选、组件、扩展、工作区、本机绑定和私密状态，只更新产品版本并重建严格启动胶囊与真实双快照。详细规则见 `core/upgrade/upgrade-1.4.1-to-1.4.2.md` 与 `core/upgrade/release-manifest-1.4.2.toml`。该历史发布保持不变。

1.4.2 → 1.4.3 修复文件切换后旧运行会话可能继续沿用旧指令链、却被误报为升级完整完成的问题。它把来源核对、文件安装、实例切换、会话激活和真实行为验收分开；有效实例在等待新任务激活或单项行为失败时继续可用，只隔离升级完成结论，只有目标文件或启动真源无效才回滚文件事务。它还要求最终用户行动回到总体目标中的下一项未完成工作。详细规则见 `core/upgrade/upgrade-1.4.2-to-1.4.3.md` 与 `core/upgrade/release-manifest-1.4.3.toml`。该历史发布保持不变。

1.4.3 → 1.4.4 加入按需 Skill 工坊：推荐不自动转换，只有用户明确选中的正式方法才在本地隔离副本中由 Agent 自动脱敏、通用化和参数化；别人分享的 Skill 先只读检查，不执行脚本、不安装依赖、不自动改写。单个包只会进入 `ready`、`review` 或 `isolated` 的局部结果，不影响 Agent Carry 主体。空模板继续为空；已有实例逐字节保留 SOP、能力、已安装 Skill、本地导出包和其他用户内容，升级本身不会生成、分享或安装 Skill。详细规则见 `core/upgrade/upgrade-1.4.3-to-1.4.4.md` 与 `core/upgrade/release-manifest-1.4.4.toml`。该历史发布保持不变。

1.4.4 → 1.4.5 修复长期对话中的升级连续性：每个新实质目标开始前只做一次极小 Agent Carry 启动基线比较；文件切换后，Agent 在当前原子操作结束的安全节点自动完成严格入口核对、最小新版规则接续、不可变宿主冲突检查和一项无破坏的代表行为验收，不把测试交给用户。普通文件重读不能冒充采用；宿主确实阻止原地接续时，只保持受影响行为待接续，有效实例和无关能力继续可用，新任务才是立即采用新版的最后兼容路线。详细规则见 `core/upgrade/upgrade-1.4.4-to-1.4.5.md` 与 `core/upgrade/release-manifest-1.4.5.toml`。该历史发布保持不变。

1.4.5 → 1.4.6 闭合 Skill 工坊的分享与接入：生成前选择 ZIP、独立文件夹、链接或本机保留；Agent 只处理所选方法的隔离副本，并从唯一可编辑真源生成真实、不覆盖的本地载体。接收方提供文件夹、ZIP 或链接后，Agent 先走有界隔离检查，再只保留一次必要的安装确认。旧 `draft`、`review`、`ready` 条目与实例自有字节保持原样，缺少新交付字段只在看板显示为“分享方式待选择”；升级不生成、分享、安装或改写任何 Skill。一个载体异常只影响当前 Skill。详细规则见 `core/upgrade/upgrade-1.4.5-to-1.4.6.md` 与 `core/upgrade/release-manifest-1.4.6.toml`。该历史发布保持不变。

1.4.6 → 1.4.7 修复复杂任务已经正确完成、却可能在工具链、错误恢复或上下文压缩后遗漏实际使用回执、阶段学习回执、文件去向或总体目标下一步的问题。一次性本地收尾契约只检查当前任务的有界事实和最终草稿；缺什么只修当前回复，不联网、不建立任务数据库、不重跑业务工作。检查器、Node 运行时或输入元数据不可用时透明降级，已完成结果、文件、对话和无关能力继续可用。空模板保持空白，已有 1.4.6 实例的全部实例所有字节逐路径、逐字节保留。详细规则见 `core/upgrade/upgrade-1.4.6-to-1.4.7.md` 与 `core/upgrade/release-manifest-1.4.7.toml`。该历史发布保持不变。

1.4.7 → 1.4.8 把首次创建、普通学习保存和共享 Skill 接入补成随包可执行的确定性本地入口。Agent 只负责自然语言交流和语义内容，产品入口负责正式字段、隔离写入、回读、幂等和回滚；共享包的脚本和依赖保持不执行。普通请求、运行时或单项派生刷新异常只限制当前创建、学习或 Skill，并向用户说明，Agent 其余部分继续可用。未经有界回读的状态保持“本次未核对”。空模板保持空白，已有 1.4.7 实例的全部实例所有字节逐路径、逐字节保留。详细规则见 `core/upgrade/upgrade-1.4.7-to-1.4.8.md` 与 `core/upgrade/release-manifest-1.4.8.toml`。该历史发布保持不变。

1.4.8 → 1.4.9 是未公开的本地候选历史：它加入同一 Skill 新版接入和问题报告入口，但从未授权替换真实实例。其详细规则仍见 `core/upgrade/upgrade-1.4.8-to-1.4.9.md` 与 `core/upgrade/release-manifest-1.4.9.toml`，不能改写成已发布事实。

AI Carry 2.0.0 是产品改名与兼容迁移的已发布历史。公开 Agent Carry 1.4.8 和本地未公开 1.4.9 是它的直接来源；它把当前产品名称和新生成身份迁到 AI Carry，同时保留实例身份、`created_from`、资产、组件、工作区、本机绑定、私密内容、Skill、报告、交接和未知字段。旧组件接口、旧快照变量与旧隐私迁移包类型继续有界可读；GitHub 仓库仍使用 `Agent-Carry` slug，远程改名是以后单独授权的外部动作。其详细规则见 `core/upgrade/upgrade-1.4.8-to-2.0.0.md` 与 `core/upgrade/release-manifest-2.0.0.toml`，历史发布边界不得改写。

AI Carry 2.0.1 是已经发布的升级确认修复历史。1.4.8、保留的本地 1.4.9 与 2.0.0 都可直接升级到它；它把动态核验时间与稳定权威指纹分开，同时保留 `confirm` 的第二次现场核对。详细规则见 `core/upgrade/upgrade-2.0.0-to-2.0.1.md` 与 `core/upgrade/release-manifest-2.0.1.toml`，该历史发布保持不变。

AI Carry 2.0.2 是已经发布的局部容错历史。它把首次创建、启动、学习、Skill、升级核心结果与可重建投影分开；升级预览只现场核验官方来源一次，复制候选启动本机工具前只检查当前组件明确使用的绑定。详细规则见 `core/upgrade/upgrade-2.0.1-to-2.0.2.md` 与 `core/upgrade/release-manifest-2.0.2.toml`，历史发布边界保持不变。

AI Carry 2.0.3 是已经发布的实例兼容协定历史。它让持久安装、更新、删除、学习、进化与母版升级共用 `core/protocols/INSTANCE_EVOLUTION_COMPATIBILITY.md`，由实例 Agent 根据真实所有权和内容选择保留、适配、重连、重建或局部停用。详细规则见 `core/upgrade/upgrade-2.0.2-to-2.0.3.md` 与 `core/upgrade/release-manifest-2.0.3.toml`，该历史发布保持不变。

AI Carry 2.0.4 是已经发布的母版自检修复历史。它让启动预算只读取 `assistant.toml` 的结构化配置，并把实例升级验收限制为事务返回的 `sessionReentryCommand` 与一项相关无破坏行为，不运行开发仓库检查全集。详细规则见 `core/upgrade/upgrade-2.0.3-to-2.0.4.md` 与 `core/upgrade/release-manifest-2.0.4.toml`，该历史发布保持不变。

AI Carry 2.0.5 是已经发布的架构减法历史。它压缩重复启动规则，把固定变更流程收敛为所有者、最小真源、受影响旅程和真实结果四项，并明确“全面思考不能变成全面管控”。详细规则见 `core/upgrade/upgrade-2.0.4-to-2.0.5.md` 与 `core/upgrade/release-manifest-2.0.5.toml`，该历史发布保持不变。

AI Carry 2.0.6 是已经发布的实例升级边界修复历史。它只修复内置治理卡的有界品牌迁移、私密迁移扫描一致性、Skill 载体缺失的局部降级，以及电脑端和维护者许可证检查边界。详细规则见 `core/upgrade/upgrade-2.0.5-to-2.0.6.md` 与 `core/upgrade/release-manifest-2.0.6.toml`，该历史发布保持不变。

AI Carry 2.0.7 是已经发布的极简看板历史。它把摘要、状态与主动作放在前面，详情按需展开，并重新归位迁移、安全、升级、私密导入导出、问题报告与 Skill 工坊入口。详细规则见 `core/upgrade/upgrade-2.0.6-to-2.0.7.md` 与 `core/upgrade/release-manifest-2.0.7.toml`，该历史发布保持不变。

AI Carry 2.0.8 是已经发布的 Skill 创作核心历史。它在现有 Skill 工坊内复用对话和正式资产，只追问真实缺口，生成开放格式，并把宿主原生 Creator 保持为可选复核；已有 Skill、实例身份、用户资产和本机内容继续原样保留。详细规则见 `core/upgrade/upgrade-2.0.7-to-2.0.8.md` 与 `core/upgrade/release-manifest-2.0.8.toml`，该历史发布保持不变。

当前目标是 AI Carry 2.0.11。1.4.8、保留的本地 1.4.9 与 2.0.0 至 2.0.10 都可直接升级。本版修复规划目录和本机文件被误判为开发仓库、缺快照不能重建、快照故障阻断有效核心接续、已成功导入却因清理失败误报失败，以及看板只读排查被误拦；包含此前本地保存的任务召回误排除修复。实例身份、习惯、记忆、SOP、能力、Skill、工作区和私密内容继续保留，未知文件不删除、不执行。详情见 `core/upgrade/upgrade-2.0.10-to-2.0.11.md` 与 `core/upgrade/release-manifest-2.0.11.toml`。本版不包含客户端试验、不增加手机端承诺，也不恢复已取消的个人写作风格专用功能。

完整机器规则见 `core/upgrade/UPGRADE-CONTRACT.md` 和对应发布清单。GitHub 自动生成 ZIP 的外层目录名和压缩容器字节可能变化，因此升级验证以正式标签、发布清单、解压后的文件集合与逐文件内容为准，不把 ZIP 文件本身的一次 SHA-256 当作永久身份。升级包仍按不可信输入检查路径安全；这些来源与完整性检查会降低风险，但不向普通用户承诺“官方绝对安全”。
